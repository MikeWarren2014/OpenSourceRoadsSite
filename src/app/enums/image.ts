/**
 * @readonly
 * @enum {string} the keys of the images map dictionary
 */
export const ImagePrimaryKeys = Object.freeze({
  // primary keys
  PEOPLE : "people",
  WORK_WEVE_DONE : "work",
})

export const ImageSecondaryKeys = Object.freeze({
  TEAM : "team",
  MIKE : "Mike",
  CHRIS : "Chris",
})
